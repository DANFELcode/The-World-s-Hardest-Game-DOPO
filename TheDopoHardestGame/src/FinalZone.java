package domain;

/**
 * Área a la que debe llegar el jugador después de recolectar todas las
 * monedas
 */
public class FinalZone extends Zone {

    public FinalZone(double x, double y, double width, double height) {
        super(x, y, width, height);
    }
}