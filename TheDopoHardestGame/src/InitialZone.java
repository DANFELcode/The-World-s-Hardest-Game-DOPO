package domain;

/**
 * Área desde la cual parte el jugador al inicio del juego o al morir[cite: 1].
 */
public class InitialZone extends Zone {

    public InitialZone(double x, double y, double width, double height) {
        super(x, y, width, height);
    }
}